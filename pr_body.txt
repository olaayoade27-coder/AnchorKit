feat: idempotent initialize tests

Added src/contract_tests.rs verifying:

- First initialize succeeds, admin stored
- Second call returns Err(ErrorCode::AlreadyInitialized), no state change
- Different admin fails on second call

Logic already correct (early instance.has(admin_key) check before any writes).

Fixed incidental compilation issues:
- symbol_short! length limits ("registered" -> "reg")
- Added ErrorCode::StorageCorrupted
- RateLimiter function signatures (owned params for Soroban compat)
- lib.rs import fix
- no_std string compat in tests

All tests pass, no regressions introduced.

Issue fixed: initialize never panics on already-initialized contract.

